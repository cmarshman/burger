<h1>BURGERS!</h1>

<h2>Burgers that need to be IN MY BELLEY</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers in MY BELLEY</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca"> Whats your burger:</label>
    <input type="text" id="ca" name="name">
  </div>
  <button type="submit" name="devoured">Add Burger</button>
</form>